Please install wxWidgets 3.0 and Boost >= 1.58.
wx-config global utility will be used to discover location of wxWidgets header/lib files.
Boost libs/headers are assumed to be in standard sytem-wide paths.

Then just run

    $ make
    $ sudo make install
    $ NetlistViewer

